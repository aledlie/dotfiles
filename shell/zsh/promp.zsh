eval PATH="$(bash --norc -ec 'IFS=:; paths=($PATH); for i in ${!paths[@]}; do if [[ ${paths[i]} == "''/Users/alyshialedlie/.pyenv/shims''" ]]; then unset '\''paths[i]'\''; fi; done; echo "${paths[*]}"')" export PATH="/Users/alyshialedlie/.pyenv/shims:${PATH}" export PYENV_SHELL=zsh source '/opt/homebrew/Cellar/pyenv/HEAD-cd2f410/completions/pyenv.zsh' command pyenv rehash 2>/dev/null pyenv() { local command=${1:-} [ "$#" -gt 0 ] && shift case "$command" in rehash|shell) eval "$(pyenv "sh-$command" "$@")" ;; *) command pyenv "$command" "$@" ;; esac }
